import React from 'react'

const Select = ({option, setOption, placeholder, options}) => {

  return (
    <div className='w-full flex flex-col gap-2'>
      {placeholder}
    </div>
  )
}

export default Select